language: node_js
node_js:
  - "12"

branches:
  only:
    - master

cache:
  directories:
    - node_modules

before_script:
  - yarn install

script:
  - yarn build --prod
  - yarn lint
  - yarn test-coverage
